name: 'Linode cli'
description: 'Make the Linode cli available to your workflow'
author: 'Brendon Lees'
inputs:
  LINODE_CLI_TOKEN:
    description: 'linode cli token'
    required: true
branding:
  icon: 'terminal'
  color: 'green'
runs:
  using: 'node12'
  main: 'dist/index.js'
